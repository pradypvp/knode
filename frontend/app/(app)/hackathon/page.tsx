"use client";

import { CommunityBoard } from "@/components/knode/community-board";

export default function HackathonPage() {
  return (
    <CommunityBoard
      category="HACKATHON"
      heading="Hackathon Teambuilder"
      subtitle="Create a listing so others can find your stack and join."
    />
  );
}
